import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.groupExpenses.app',
  appName: 'group-expenses',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
